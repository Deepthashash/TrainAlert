import 'package:flutter/material.dart';
import 'package:train_alert/screens/auth/signIn.dart';

class Auth extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: SignIn(),
    );
  }
}